var path = require("path")
const fs = require("fs")
const userPath = (path.join(__dirname, "..", "customsounds"))

module.exports = function intro(bot, msg, name, url, bool){

    //fs.writeFile()
    if (err) throw err;

    var intro;

    bot.introSound.set()

}